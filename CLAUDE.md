# football-decision

Текстовый прототип футбольной decision-RPG. Язык игры — украинский; язык кода, комментариев,
тестов и README — русский. Обоснования решений живут в комментариях кода и README; `DESIGN.md` —
целевая картина и дорожная карта (M1..M5), правки сверяются с ней, а не с последним логом.

## Правила

- **Никаких процентов и вероятностей на игровых экранах и в текстах эпизодов.** Игрок видит ярлыки
  («упевнено / ризиковано / відчайдушно», «утримати / створити / вирішити») и модификаторы после броска.
  Тест `tests/acceptance.test.ts` это проверяет.
- **После любой правки `src/engine/balance.ts` или `src/content/episodes.json` — `npm run sim -- 2000`.**
  Три проверки должны быть «ок»; цифры в README обновить.
- **Имена игроков в контенте — только плейсхолдеры** (`{partner}`, `{striker.dat}`, `{them.striker}`).
  Формы падежей — в `src/content/roster.json`. Зашитая фамилия валит тест.
- **`past` и `recap` в эпизоде не должны повторять друг друга:** `past` — поступок
  («пробив з льоту»), `recap` — последствие («і м’яч влетів у дев’ятку»). Из них собирается пересказ матча.
- Атрибуты опций — из десяти (`Attribute` в types.ts); каждый атрибут должен использоваться хотя бы
  одной опцией (тест), иначе он мёртвый на карточке. **Швидкість — ривок на десять метрів; витривалість —
  повторне або довге зусилля** (бігти назад через усе поле, тягти навздогін до останнього, пресинг утретє, другий
  темп, «витратити сили на зараз») — M9.7, 20.09: 9 таких варіантів перетеговано з pace, у витривалості 16 варіантів,
  не тільки «берегти сили». Витривалість — фізика: `optionCost` додає `physicalExtraCost`, Тіло її бачить і чує.
  **Спокій має свій приз** (`COMPOSURE_CALM`): на варіанті Спокою не діє «після провалу» −1, а його провал не
  збиває кураж (fail 0, катастрофа −1) — до того 69 варіантів composure були «пропустити хід».
- Эпизод: 3–4 опции, в каждой все четыре исхода, минимум две разные формы риска,
  `effect: great` — только там, где чистый исход действительно решает эпизод (гол/ассист).
  **Гол на clean у «упевнено» — только в `fin_*` и на `positioning`** (ниша «голова забиває без ризику»,
  решение 20.09); остальное — риск или цепочка в удар (`ep_loose_scramble`: підбір → `fin_shot`).
- **Мёртвые кнопки** (`npm run audit`, тест в content.test.ts, пороги `AUDIT_LIMITS` — храповик, только вниз):
  опция, которая не хуже соседа по обеим валютам, катастрофе и силам, — доминируемая; эпизод с отрывом EV
  лучшей опции > 1.5 — «с правильным ответом». Чинить не нерфом победителя, а призом проигравшему, которого
  у победителя нет: флаг про людей, `momentum`, `corner`, цепочка. Аудит 20.09: 34% / 36 на старте → 26% / 23 после
  пяти партий (пороги 27% / 24). **Системно:** чистый исход на ризику даёт кураж сверх яруса (`MOMENTUM_BY_BOLDNESS`
  +1 / +2) — не добавлять третий приз за смелость, трибуни уже реагируют через `boldnessHype`; **«упевнено» с асистом
  или голом команды на clean — складність ≥ 1** (гол партнёра зависит от партнёра); **у вариантов за 7+ сил исходы не
  снимают больше −2 сил** — цена риска уже в цене варианта; дорогие варианты — 7–9 сил, 10–12 только у самых
  физических (`physicalExtraCost` 6 держит критерий стамины).
- «Відчайдушно» — редкость: не больше одной такой опции на эпизод и не в каждом эпизоде.
- Форма риска = полоса катастрофы по сырым кубикам (`CATASTROPHE_BAND`), скилл её не выкупает.
  Любая правка, из-за которой сильный атрибут делает риск безопасным, откатывается.
- **Складність** (`EpisodeOption.difficulty`, −3…+4, `balance.ts:DIFFICULTY`) — вторая ось: форма —
  цена ошибки, складність — насколько трудно сделать. Двигает пороги чистого/«але»
  (`tierFor(..., difficulty)`), полосу катастрофы — нет. Обязательна у каждой опции (тест); в эпизоде
  ≥2 разных цели среди безусловных опций; цель ≥ 20 — только у `standard`/`great`. Ориентиры:
  откатити/притримати/промовчати −3…−2, стандартное действие 0, удар здалеку/черпак/п’яткою/паненка
  +2…+4. На кнопке и экране броска — `cleanTarget(position, difficulty)` / `Resolution.target`,
  не `THRESHOLDS[position]` напрямую.
- **Каждый `cost`-исход содержит осязаемый плюс** (кутовий, ключовий пас, єдиноборство, командний гол
  или ≥3 к доверию/трибунам) и конкретную цену. «Ничего не случилось, +1» — это `fail`, а не `cost`.
- Реплики второго голоса после броска — `src/content/flavor.json`: правило = `when` (+ `family`/`phase`
  сцены) + `voice` + `lines`. **Говорящих восемь** (`flavor.ts:FLAVOR_VOICES`, тест): шесть голосов картки
  (ЕГО/КОМАНДА/СПОКІЙ/БАЧЕННЯ/ІНСТИНКТ/ТІЛО) + ТРЕНЕР + ТРИБУНИ — те же сущности, что на листе, в своих цветах.
  Предметов-говорящих (СУДДЯ/ТАБЛО/ГОДИННИК/ВОРОТАР/ПАРТНЕР/СУПЕРНИК/КУРАЖ) больше нет (20.09, пользователь:
  «выглядит мусором» — одна капитель означала две системы); их строки переподписаны: партнёр → Команда,
  кураж/табло → Его, годинник → Спокій, суперник/суддя → Бачення, чужой воротар → Інстинкт, свой — Команда.
  Новая строка пишется от лица одного из восьми: наблюдение, которое этот голос мог бы сделать.
  Пул — все подходящие правила с весом 3^ключей, виденные в этом и последних матчах строки уступают
  свежим (`session.flavorSeen`, `history.recentFlavor`); строгое «самое конкретное» давало 34% повторов
  за сезон. У каждой семьи ≥3 строки на каждый исход (тест). Тон — Disco: наблюдение, не оценка; без «!»;
  строка не утверждает событий, которых нет в исходе (гол, замена, вторая жёлтая); плейсхолдеры ростера.
- **Лента между эпизодами — `src/content/feed.json`** (`engine/feed.ts`), правило = `kind` + `when` (как у
  flavor, плюс `venue`/`weather`/`strength`/`flags`) + `lines`; выбор тот же (`pickFresh`: 3^ключей,
  виденные уступают свежим, `session.feedSeen` + `history.recentFeed`). Виды: `filler`, `goalUs`/`goalThem`
  (гол ленты, с манерой, обязателен `{scorer}`), `goalUsEcho`/`goalThemEcho` (гол из исхода эпизода —
  только автор и счёт, манеру описал исход, спорить с ним нельзя), `halftime` (`{score}`), `knock`,
  `kickoff`. У каждого вида есть безусловное правило; флаги в `when` — только `them_*` и системные.
  Строки-константы в `match.ts` не возвращать: 12 строк давали 38% повторов внутри матча.
- **Стрічка між матчами — `src/content/posts.json`** (`engine/posts.ts`, экран `PostsScreen` после таблицы; **вид —
  застосунок «Y»** по макету «Стрічка як застосунок», 19.09: без рамки браузера, анатомия поста X, системный шрифт,
  компоуз ответа с нитью треда; аватары — `public/img/avatars/<handle>.webp`, без файла — буква на круге, Реєс —
  портрет с картки):
  пародия на твиттер, `accounts` + `posts` `{ group, account, when, lines, reply? }`. Группы и квота
  (`postQuota(season)`: первый сезон — про нас 3, общих 7; со второго — `POST_QUOTA`): `self` 3 (болельщики о персонаже — по факту матча: гол, оценка, доверие, флаги,
  доминирующий голос карьеры), `league` 2 (таблица, следующий соперник по `them_*`), `world` 2
  (большой футбол — **реальные клубы можно, реальные люди — только под искажёнными, но узнаваемыми
  именами**: Риштиану Коналду, Рабріціо Фомано, Джанні Ніфантіно, Пеп Гвардеола — решение 19.09;
  тест держит список настоящих написаний и валит их;
  Россия — аккаунт `rufootball`, жёстко, но про институции), `cross` 1 (пересечение миров),
  `meta` 1 (FM, Disco Elysium, EA). Выбор — `pickFresh`, память `history.recentPosts`; `when.week: [ids]` — дела прошлой недели
  (`career.weekLog`), так стрічка отвечает на побачення/подкаст/план тренера. Клубы таблицы и
  последний соперник — через `extra` (`{leader}`, `{bottom}`, `{last}` + `.gen`); у клубов только
  nom/gen — после «з/із/у» ставить «проти „{x.gen}“», не «з „{x}“». Пост не утверждает событий,
  которых не было (вторая жёлтая — нет; «суддя пояснив, що друга буде» — да). У каждой группы —
  безусловные правила, иначе к 8-му туру пул сохнет (тест «за сезон без повторов»).
  **Тон фанатов — как пишут**: с маленькой, без тире, «шо», КАПС после поражения, ))); издания и
  пародии — чисто. Виды: `kind: poll` (+`poll: []`), `deleted` (оригинал — в `reply` через `{quote}`),
  `promo`, `live` (+`live: [от, до]`). **Ответ игрока** — `replyOptions: [{ text, reaction, effect }]`,
  `effect` как у дела недели; один на стрічку, подмешивается с `REPLY_CHANCE`; применяется через
  `applyWeek`, который дополняет `career.nextMatch`, не затирает (тест в posts.test.ts). Экран
  `LevelUpScreen` — только при `BALANCE.growth.levels` или `unspentPoints > 0`. **Момент матча**:
  `when.moment: worst|best` + `{moment.minute}`/`{moment.ord}`/`{moment.acc}`/`{moment.past}`/`{moment.recap}`
  (`pickMoments` в match.ts → `OurResult.moments`); ключи плейсхолдеров — только строчные латинские
  (`fillNames` иначе их не видит); один пост о моменте на стрічку гарантирован.
- У каждого варианта есть `voice` (кто и что говорит), голос слышно только сильный — см. `voices.ts`.
- **Голос бачить** (`EpisodeOption.insight`, Disco Elysium: пассивная проверка). Сильный атрибут
  (`BALANCE.insightMinMod`, `voices.ts:voiceSees`, только атрибутные голоса) замечает деталь, которой
  нет в сетапе, — строка над вариантами — и только из-за неё появляется вариант; слабый игрок его не
  видит и не знает, что он был. `insight.line` — факт, не совет, без «!»; `voice.who` тот же;
  атрибут броска — чувство этого голоса. Вариант со вставкой — сверх 3–4 безусловных (не больше одного
  условного на эпизод), бонусов к броску не даёт; **его выигрыш — складність ≤ −2 при той же форме**:
  знание делает действие проще, не безопаснее (тест: цель ниже соседей той же формы и масштаба). `availableOptions` без
  `player` такие варианты прячет. Стартовый Реєс видит Бачення и Інстинкт, не Тіло/Спокій (голос composure переименован 19.09: «Холоднокровність» не помещалось).
  **Его и Команда бачать по состоянию матча** (`voiceSeesNow`): Его — на своей серии или кураже ≥
  `egoSeesMomentum`, Команда — на серии или доверии ≥ `teamSeesTrust`; так дело недели «Его гучніше»
  (стартовая серия) открывает вставку Его в первом эпизоде. На кнопке — чип «відкрив <голос>», после
  броска — строка «<голос> побачив цей варіант»: игрок должен связать кнопку с голосом, не с удачей.
- **Флаги недели в сценах** (`week_set_pieces`, `week_tactics`, `week_promise`): дело ставит флаг с
  правилом в `flags.json`, а в эпизоде есть вариант с `requires.flags` (чип «з тижня») и сетап с
  `when.flags`, который прямо ссылается на тиждень («у вівторок ви прогнали його сорок разів»).
  Вариант обязан снять флаг во всех исходах. Тест «правило никто не ставит» считает и дела недели.
- Рост персонажа (M2) — только через `career.ts`: `effectivePlayer()` для матча,
  `applyMatchToCareer()` в конце, `consumeStartPenalty()` в начале следующего. Не трогать
  `PLAYER.attrs` напрямую и не завести второй источник правды для «текущих атрибутов».
  **Очко = +1 к модификатору** (`POINT_VALUE` = `ATTR_MOD.step`), не +1 к значению.
  **Уровни выключены** (`BALANCE.growth.levels = false`, решение 17.09): опыт и уровень считаются,
  очков не дают; рост — только тренировки недели (`trainToPoint` = 3). Код уровней и `LevelUpScreen`
  оставлены, включаются одним полем; миграция карьеры фантомных очков при выключенных уровнях не даёт.
- **Тиждень між матчами** — `engine/week.ts` + `content/activities.json` (74 дела), экран `WeekScreen` —
  **зошит Реєса** (19.09, макет «Тиждень у зошиті» А v2): один розворот у лінійку замість чотирьох екранів; від руки
  Neucha — тільки коротке (стікери голосів на скотчі, обраний обведений, інші відірвані; рядок-наслідок під днем;
  список «до матчу» з галочками), малюнки на полях `ui/doodles.tsx` по сиду тижня. **Ісход і сцена — лист вечора**
  (20.09, макет «Лист вечора»): той самий лист моменту з матчу (`.moment`, ярлик «ДЕНЬ N · ВЕЧІР/ПІЗНО», сетап Lora,
  голоси колонкою, «Твій хід»), вписаний під день, зошит навколо гасне (`.nb-book.dimmed`); **не модальне вікно** —
  у грі їх немає. Довга проза від руки читалась погано і не мала автора; у листа автор — оповідач гри. Кнопка
  знизу — наша; поки лист відкритий, кнопка всередині листа. **v3 (19.09):** `BALANCE.week.days` × `perDay` предложений (`offerWeekDays`: голоса в дне
  разные, ≤ `maxPerVoice` на неделю, при нехватке голосов лимит снимается — закрытый город), одно дело
  в день, у дела `outcomes` (1–3, `ActivityOutcome`): вес × `outcomeVoiceBoost` если `voice` —
  доминантный голос карьеры (`dominantCareerVoice`, ≥ `dominantMin`) × `outcomeCheckPass/Fail` по
  `check {attr, min}` (модификатор) × `outcomeSituationBoost` если `boost` (условие `ActivityWhen`)
  совпал. Исходы считаются в `planWeek` тем же rng, что предложения (перезагрузка не перебрасывает);
  применяется эффект исхода вместо `effect` дела (`withOutcome`). `followUp` — id сцены из
  `content/weekscenes.json` (`WeekScene`: setup + 3–4 варианта, ровно один с `insight {who, line}`,
  показывается только если `weekVoiceSees(who)`: атрибутные — `voiceSees`, Его — трибуны ≥ 7 или
  доминантный, Команда — доверие ≥ `teamSeesTrust`); одна сцена на неделю **и без дословных повторов:**
  `sceneFor(outcome, scenes, seenScenes(career), sceneUsed)` пропускает сцену, уже виденную в карьере (`weekLog.scene`),
  пока есть невиденные (20.09: без памяти лікарня выпадала в 55% сезонов; вес её исхода `promise` 2 → 1). Частоту
  мерить `npx tsx tools/scene-rate.ts 300`: ~1 сцена за сезон, повторов внутри сезона 0%. `neglectPenalties`:
  голос, предлагавший `neglectWeeks` недель подряд без выбора, — `quieter` на матч
  (`career.voiceNeglect`). Всё закрывает `finishWeek` (isходы + сцена + обида → `applyWeek` →
  `recordWeek` с `outcomes`/`scene`). **Правила исходов:** текст — 1–3 предложения с иронией, `note`
  обязателен, эффект обязан оставить след (тест `week3`), ⅔ дел — с развилкой, ни один исход не
  «просто хорошо/плохо» без цены или плюса; `train: "choice"` сохраняется в исходе, иначе выбор
  атрибута на карточке пропадёт. Постов между днями нет (убраны 19.09: экран с одной новостью — лишний шаг).
  `offerWeek` (шесть по голосу) остался для тестов и старых прогонов.
  **Ключові моменти канви — листами-якорями, не рядком у зошиті** (22.09, пользователь: `aside` під шапкою губився серед
  стікерів): `week.ts:ANCHOR_SCENES` — за туром (Хантер після 2-го, зимовий дзвінок після 5-го в обох сезонах, Ларссон,
  «місто питає») або за станом арки без туру (водій десятого — стан 2, кіоск — стан 3, лише перша ліга); один якір на
  тиждень, тур важливіший за стан, кожен — раз на кар’єру (`seenScenes`). У сцени є `tab` (ярлик листа) і `head`
  (заголовок дня). Новий момент сценарію — сюди, а не новим полем `aside` (його більше немає, як і `city.json`).
  Старое (v2) ниже остаётся в силе. Дело = «какой голос беру на матч»:
  `louder`/`quieter` — ±1 к модификатору атрибутов голоса на один матч (`career.nextMatch.attrBonus`
  → `effectivePlayer(..., matchBonus)`), `stamina`/`composure`/`fanHype`/`momentum` — старт
  (`Carryover.startDelta`), `coachTrust` — сразу, `train` — счётчик, `trainToPoint` = +1 очко навсегда,
  `flags` с `after` — отложенные (`CarriedFlag.after`, `Mark.whenText`), `heal`. `note` — строка
  брифинга, обязательна. Его/Команда атрибутов не имеют: `louder` — матч начинается с их серии
  (`Carryover.voiceStreak`), `quieter` — голос замовк на `week.muteEpisodes` эпизодов (`voices.muted`).
  Закрытый город оставляет ситуационные дела по `when.flags`. Флаг из матча и отложенный с тем же id
  не дублируются (`applyMatchToCareer`, `createMatch`). Дело Его с «Его гучніше» в `note` обязано иметь
  `louder: ["ego"]` — иначе вставки Его не откроются, а бирка соврёт. После разгрома или при низком доверии `coachLocksCity` — только Тіло/Бачення/
  Спокій. **Ирония в `line` — фишка игры**: одна строка, наблюдение, без «!»; дело без
  строки-наблюдения в пул не попадает. Ситуационные дела (травма, флаг партнёра, низ таблицы) —
  `weight` 8, чтобы всплывали. Балансировать `npm run sim -- --weeks 150`: разрыв политик недели ≤ 15%.
- **Пролог — тиждень нуль** (M12, 20.09, `engine/prologue.ts`, `content/prologue.json`, `ui/PrologueScreen.tsx`, макет «Пролог у
  зошиті»): нова кар’єра (`prologuePending`: без `career.prologue` і без матчів) починається з трьох розворотів зошита —
  Скаут / Дзвінок / База. **Оповідач говорить листом** (той самий `.moment`, перед вибором, а не після, як у тижні),
  **Реєс відповідає стікерами голосів** — у зошиті прози від руки немає (решение пользователя). Обраний стікер = характер
  на старт: голос гучніше (`voiceStreak`/`attrBonus`), `point: "choice"` — атрибутний пункт назавжди (не тренування),
  довіра ±5, `sub_threat` («ще до сезону»), `bond` — партнер, Спокій нижчий. **Спокою серед стікерів немає** — за лором
  голос, якого Реєс не чує. **Жарт Тібо про мафію — не флаг і не гілка**: відповідь у `career.prologue.base`, звідти її
  читатимуть репліки Тібо; гра не пояснює, що це жарт, — зрозуміє гравець. Наслідки — через `applyWeek`; `benched = true`
  примусово. Нотатки тижня/прологу з плейсхолдерами — імена підставляє App при переході в брифінг.
  **Розворот закінчується розв’язкою, а не паузою** (21.09, решение пользователя): у стікера є `reply` — реакція
  співрозмовника на тон (тон = голос відповіді: Его дерзко, Команда м’яко, Бачення питанням, Інстинкт коротко, Тіло про
  ногу) і факт «що далі» («Понеділок, дев’ята. Форму видадуть»); фаза `reply` у `PrologueScreen` показує її листом після
  «Так і відповісти». **Луна:** `sheetBy[voice]` — варіант листа наступного розвороту за голосом попередньої відповіді
  (тренер чув «через рік», Тібо чув, що ти сперечався; `prologue.ts:sheetFor`), листи починаються мостиком («Той інший
  номер — Мендонса, тренер…», «Понеділок, дев’ята. База — на кінцевій десятого трамвая…» — трамвай №10 як знак статусу,
  без окремих кроків історії). **Тон живе перший матч**: системні маркери `call_tone_ego/team/vision`
  (`career.ts:prologueFlags`) → сетапи `ep_bench_call` і репліки ТРЕНЕРА. **Луна агента:** `career.agentEcho`
  (ставить `resolveAgent`, знімає `applyMatchToCareer`) → маркери `agent_left/stayed/waited` на перший матч сезону
  (сетап розминки, ТРЕНЕР, Тібо «мафія чула про дзвінок») і рядок програмки (`programme.ts:arcLine`). Новачку не знайомі
  імена — на листах ролі: «Мендонса, тренер», «Ларссон, його гравець на твоїй позиції».
  **Перший матч** (`content/firstmatch.json`, `Carryover.tutorial` → `session.tutorial`): з лави, чотири фіксовані сцени
  замість плану (`ep_long_range` форма ризику → `ep_progressive_pass` кубик і атрибут → `ep_second_wave_run` голоси, Тіло
  перше → `ep_late_captain` «вийшло, але» і тренер); реактивні сцени в першому матчі не спливають (дублер із прологу чекає
  другого). **Підказки — прожектор** (`ui/Spotlight.tsx`, макет «Здобутки і підказки» А, решение пользователя 20.09):
  портал у body — затемнення з діркою (clip-path evenodd), золота рамка навколо цілі (`target`: choices / voices на
  `EpisodeCard`, formula / verdict на `RollView` після повного показу), виноска на кремовому папері з «Зрозуміло» і «n / 4»;
  тапом мимо не закривається; слова-ярлики в кольорі форми. Курсив під сетапом (`.tutor`) відкинуто: читався як частина
  сцени і говорив про дужки, яких ще не видно. Вимикач «Підказки в першому матчі» в налаштуваннях (`settings.hints`).
  **Здобутки** (`ui/LootSheet.tsx`, той самий макет, варіант Б): підсумок тижня і прологу — не список із галочками ручкою,
  а лист оповідача поверх погаслого зошита (зошит — рука Реєса, здобутки видає гра): `applyWeek`/`finishWeek`/
  `finishPrologue` повертають `loot: LootItem[]` (kind / who / dir / where / progress / attr) поруч із `tags`; речі лягають
  по одній (stagger, при «Менше руху» — без), «назавжди» — золотом, під списком чотири бокси картки з ▲ у зміненого,
  кнопка всередині листа. `WeekScreen.onFinish` → `WeekResult { loot, before, after }`.
- **Сцена агента** (M12, `engine/agent.ts`, `content/agent.json`, `ui/AgentScene.tsx`): вердикт «Дзвонить агент» на ESPM
  не чіпати — це нагорода, персонаж цього хоче; «Новий сезон» після нього веде в сцену (лист оповідача: сетап, Его і
  Команда, три відповіді). **«Так» зривається за обставинами сезону** (`collapseReason`: травма → медогляд, лідер → клуб
  під заставою, інакше → скаут пішов) — з іронією, без покарання: Его гучніше на перший матч, довіра й трибуни не падають.
  «Ні» — трибуни, Команда, тренер; «зачекати» — Спокій. Наслідки — через `applyWeek` у `nextMatch` першого матчу нового
  сезону; вибір і причина — `career.agentLog` (для стрічки й сцен тижня). Ціль кар’єри не декларується: те, що тримає
  Реєса тут, — у тканині сезону (M11), сцена лише показує, що вийти неможливо, і не пояснює чому.
- **Арка персонажа** (M13, DESIGN.md, 20.09): `career.ts:arcStage` — стан 1 «не зламатися» → 2 «помітили» (`BALANCE.arc.noticedFrom`
  матчів) → 3 «свій» (`ownFrom` матчів + трибуни ≥ `ownHype` або дует ≥ `ownBond`) → 4 «не помітив, коли» (після сцени агента).
  Стан ніде не показується — він у `state.arc` (через `consumeStartPenalty`), `WeekContext.arc`, `ProgrammeInput.arc`,
  `WhistleWhen.arc`; умови `arcMin`/`arcMax` є в `SituationWhen` (репліки, сетапи, стрічка) і `ActivityWhen`. Без стану
  (прогін, тести) правила з arc не підходять. **Правило контенту: усе про від’їзд — абстрактно, усе про місто — предметно;**
  жодного «ти зрозумів, що щастя тут» у будь-якому голосі; персонажу — один рядок у зошиті, довше — оповідач. Скелет (20.09):
  репліки Тібо в `flavor.json` (КОМАНДА, `{dm}`: страшні до стану 2, сильніші з маркером `tibo_asked` — системний флаг
  з `career.ts:tiboFlags`, коли в пролозі «перепитав», живе до стану 3; смішні від стану 3 — жарт спільний); свисток у
  чотирьох регістрах (`when.arc`); програмка — «Улюбленець трибун» від стану 3, тільки з уст прес-служби; справи
  `walk_base_1..3` («Дорога на базу», по одній на стан — під ноги → кава без питань → питають про чуже коліно);
  `tibo_newbie` (once, від стану 3) → сцена `sc_tibo_newbie` — Реєс по цей бік жарту. **Решта (20.09, cdd8612 →):**
  стрічка «Y» — пости `self` з `arcMin`/`arcMax` («хто це» → «той з коліном» → «наш» → «взимку ходили чутки»),
  `PostContext.arc`; `lastWeek` тепер містить і ісходи (`interview:dream_ego`) — так стрічка повторює, що Реєс сказав;
  ESPM — колонка про Реєса за станом (`content/espm.json`, `espm.ts:playerColumn`, віджет `.espm-col` над рекламою, пам’ять
  медіа спільна з постами); Спокій у стані 1 чутно лише сильним атрибутом (`voiceAudible`), перша репліка — факт про ногу
  (flavor `arcMin: 2, arcMax: 2`); Тіло про ногу — лише в стані 1; Мендонса «дивиться, записує» (стан 1) → «не дивиться —
  перестав перевіряти» (стан 3); справи `interview` (від стану 2; ісход голосом Его — абстрактно, Команди — предметно),
  `sub_advice` (від стану 3 при `sub_threat`, знімає його), `psych_fear` / `psych_noticed` / `psych_home` (психолог за
  станом — внутрішні переживання: страх зникнути із заявки → «не хочеш, щоб перестали впізнавати» → «перед сном думаєш не
  про удар», без висновків; `psych_home` — від 6-го туру). **Літній дзвінок** (`agentPending` → `'summer'`, коли в
  `agentLog` є попередній сезон): обставин немає, два варіанти; «так» → `career.ended`, епілог (`agent.json:epilogue`,
  останній свисток у чужому місті), меню показує епілог і «Нова кар’єра»; «ні» — нічого не стається. Партнер при дуеті
  додає голос у сцену агента (`agent.json:bonded`).
- **Дві ліги, відпустка, фінал** (M14–M16, 21.09, сценарій користувача; DESIGN.md): кар’єра — **два сезони**. Календар
  серпень → травень (`season.ts:MONTHS`, `monthOfRound`), 10 турів, після 5-го — **зимова перерва** (`WeekContext.winter`,
  дела `when.winter` в `activities.json`, чутка агента рядком `aside` у зошиті, без сцени). **Перший сезон — друга ліга**
  (`leagueOf(number)`), мета клубу декларується (програмка `.prog-goal`, ESPM про зону підвищення) — на відміну від мети
  Реєса. **Регламент** (`season.ts:promotion`): трійка — `earned`, нижче — `scandal` (новина ESPM: нагору йде стільки
  команд, яке наше місце), з нами в наступний сезон — максимум `PROMOTED_WITH` = 2 (`createSeason(..., keep)`), решта —
  клуби вищої ліги (`roster.json` `tier: "top"`, `OPPONENT_KEYS.top/second`; тести й сим — `second`, `sim --top` — вища).
  Скандал лишає слід: `career.promotion` → пости `promoted_scandal/earned`, програмка «за регламентом», `fanHype` на старті
  нижче (`scandalHypeDrop`). **«Зустрічалися торік»**: `career.lastSeason.results` (`metLastYear`) → рядок програмки,
  `nextFlags: met_last_year` у постах, флаг матчу `met_last_year` (системний, ставить App). **Кінець травня S1** — дзвінок
  безумовний (`firstSeasonVerdict`: формулювання за таблицею/протоколом/«помітили», лава — окремим рядком); сцени агента
  в першому сезоні немає (`agentPending` → null). **Відпустка** (`engine/vacation.ts`, `content/vacation.json`,
  `PrologueScreen` з `header/lootTab/labels/arc`): дзвінок → **травма на індивідуальному тренуванні перед медоглядом —
  єдина причина зриву** (борги прибрано: клуб із боргами продає першим), травма завжди, вибір — як готувався
  (`injury: heavy/medium/light` → `injuredMatches`; Спокій «з фізіо» — `arcMin: 2`) → база: **дублер пішов** у перший із
  `promo.with` (`career.subLeft/subClub`; `content:syncRoster` підміняє ім’я `sub` на місці — Марен, `{oldsub}` — Ларссон
  для програмки/постів/сетапу `ep_shoulder_duel` за флагом `sub_there`; акаунти `@thibault_6`, `@larsson_10` у Y);
  `agentLog` отримує `{season:1, choice:'leave', reason:'medical'}`, тож стан арки 4 — не одразу, а з `arc.settledFrom`
  (15 матчів). **Фінал** (`engine/ending.ts`, `content/ending.json`, після другого сезону, `secondSeasonVerdict` «Дзвонить
  скаут»): варіанту лишитися немає — три розвороти «як прощатися» (останній вечір / інтерв’ю ESPM / що взяти з міста),
  лист дзвінка — за голосом відповіді скауту в пролозі (`firstVoice`, рима «через рік подзвоните самі»), партнер без
  дуету — `replyCold`, епілог замість здобутків (`epilogue`), `career.ended` + `career.ending`, меню показує епілог і
  «Далі буде». Правило M13 «від’їзд абстрактно / місто предметно» — тест: у відпустці й фіналі немає назв клубів.
- **Краї плівки** (`ui/Film.tsx`, 20.09): 10 px по краях вікна на всіх екранах гри (титул, матч, зошит, програмка, картка,
  налаштування) — рівно зазор між листом і краєм, нічого не перекривають і на телефоні; підписи (кадр, назва, слот) — тільки
  на титулі, решта — сама перфорація (решение пользователя). Немає на дошці, ESPM і стрічці — «чужі» екрани. **Кнопка
  наступного кроку прилипає до низу** (`.menu-primary`, `.nb-sheet-btn`, `.de-next`, `.whistle-go`, `.prog-cta` —
  `position: sticky`), тому `.moment` і `.nb-book` — `overflow: clip`, не `hidden` (hidden робить контейнер
  scroll-контейнером, і sticky не працює).
  Свисток: `WhistleWhen.first` — рядки «first» витісняють решту (`pickWhistleLine`), уперше про віру собі. Тест
  `firstmatch.test.ts` тримає: епізоди безумовні, влазять у слоти ≥ `bench.entryMinute`, друга сцена (перша на полі) — усі три
  форми, Тіло не говорить до четвертої.
- **Тема и экран матча (19.09, макеты А2/Г):** токены в `styles.css :root` (`--font-text` Lora,
  `--font-mono` IBM Plex Mono, `--font-caps` Oswald, `--v-<voice>` цвета голосов); шрифты локально
  в `public/fonts` + `public/fonts.css` — не подключать Google Fonts по сети (APK офлайн). Матч:
  `MatchScreen` (поле `Pitch`, стрічка-диалог, зона «на кубик» из `stateMods`), `EpisodeCard` — **лист момента** (19.09, макет «стало+ зі знаком»): ярлык минуты на кромке,
  сетап, голоса колонкой с линией, варианты с номером в ячейке `[форма ціль] ярлык` (на кнопке нет
  атрибута/бонуса/цены — решение пользователя); **аффордансы (19.09, макет «Лист моменту: де кнопки», вариант А,
  после плейтеста «не нашёл кнопки»): голоса без коробки курсивом, заголовок «Твій хід», варианты — тёмная полоса с
  рамкой, действие первым, скобка вторым рядом, номер кремом, стрелка справа, чипы «на кубик» без рамок** — инертное
  не должно выглядеть нажимаемым; **два стани екрана (22.09, макет «Лист моменту за згином»): поки лист відкритий (сцена, **кидок**, вихід,
  свисток) — замість поля смужка `.score-strip` (клуби, великий рахунок кольором за станом, лінія сил), стрічки нема; поле й стрічка
  повертаються після «далі», розв’язка грає при монтуванні поля з місця останньої сцени (`pitchEpisode`); скобка варіанта в рядок з ярликом, коли влазить (`.choice-text` flex-wrap), голоси й
  «Твій хід» щільніші відступами — щоб лист із варіантами влазив в один екран**; лист светлее фона с зерном 10.5%, светом от угла и водяным
  знаком штрафной (`.moment::before`), лента при открытом листе приглушается (`.tape.dimmed`), `RollView` (кубики-барабан с раздельной остановкой → сумма → чипы факторов и тикающий итог →
  формула → вердикт-штамп → исход; без баннера/таблицы/зоны «на кубик» — решение 19.09). Новый модификатор в `context.ts` — сразу с `short` (одно-два слова для чипа «на кубик»,
  19.09: чипы словами, без иконок; у правил `flags.json` — поле `short`, без него берутся первые два слова
  label). Поле (`Pitch.tsx`, 19.09): 11 на 11, наши 4-3-3, соперник по `strength` (weak 4-4-2, even 4-2-3-1,
  strong 4-3-3), фаза сдвигает линии (`PHASE_SHIFT`), Реєс — в точке сцены по `family`/`phase`
  (`FAMILY_SPOT`), ближайший соперник рядом; точки едут (`.pitch-dot` transition, при «Менше руху» — нет);
  **три слоя движения (19.09):** розв’язка по исходу (`engine/finale.ts: finaleFor` → `Pitch.finale`, запуск со
  штампом `RollView.onVerdict`: гол/пропустили/сейв/мимо/пас/втрата/фол/red/дуель/пульс — хореография 3–4
  актёров), лента (`Pitch.pulse` — последняя строка ленты разыгрывается 2–3 передачами, гол в ленте — в
  сетку), дыхание (раз в 4 с ±2%, только когда никто не бежит). Новая семья — координата в `FAMILY_SPOT`;
  когда появится Phaser, заменить на `zone` эпизода. **Критический успех** (две десятки) — штамп «Критичний
  успіх» золотом, золотые кубики; механика без изменений (`tierFor` → clean, `pickOutcome` → `outcomes.crit`). Сили — полоса по
  нижней кромке поля (`.stamina-bar`), не в зоне «на кубик». Остальные экраны — старая структура в
  новой палитре, переводить по одному.
- **Титул «Inside the Box»** (19.09, `ui/TitleScreen.tsx`, роут `#/`; игра — `#/play`, слоты `#/slots`,
  налаштування `#/settings`, про гру `#/about`): картинка `public/img/title.webp` (Gemini, кроп без мяча,
  притушена под тему — так Реєса видит Его), логотип `ui/Logo.tsx` (SVG, название внутри штрафной,
  леттеринг `--bad` — цвет штампа, намеренно), реплика голоса под названием — `content/title.json`
  (`engine/title.ts`, «ЕГО — …», **без скобки проверки** — убрана 19.09, ничего не сообщала; условия
  career/last/trust/idle/over, память — `settings.ts`, общая для устройства). Один primary: «Продовжити»
  с корешком удостоверения активного слота или «Нова кар’єра». **Слоты** — `telemetry/slots.ts` +
  `saves.ts`: три карьеры, нулевой слот пишет в старые ключи; смена слота = `key` у Game.
  **Налаштування** — `telemetry/settings.ts` (кидок барабан/одразу, вібрація на штампі, менше руху,
  розмір тексту через `zoom`), классы на `<html>`; «Вивантажити логи» живёт тут, не на итоге матча.
  Тексты титула — ирония, без «!» (тест).
- **Брифинг = програмка матчу** (19.09, `ui/BriefingScreen.tsx`, макет «Брифинг як програмка»): кремовый лист
  с зерном — единственный светлый экран; рубрики Реєс / Суперник / Стадіон / Погода / Форма / Слово тренера
  (цитата в рамке). Заметка «Реєс» — `engine/programme.ts: programmeNote` прозой без чисел, кроме счёта и
  вех: прошлый матч, серия, тиждень (названия дел из `weekLog`), статус у тренера по довірі. Черта соперника —
  `traitNote` голосом клуба, одна фраза с пометкой «увага»; вратаря не выдавать (награда Бачення). Тон
  официальный, без иронии — програмку пишет пресс-служба. Абзац-объяснение из меню кар’єри удалён.
  В исходниках экранов нельзя `%` даже в комментариях — тест «никаких процентов» грепает файл целиком.
- **Фінальний свисток (M10, 20.09, `engine/whistle.ts`, `content/whistle.json`, `ui/WhistleCard.tsx`):** после последней строки
  ленты — лист оповідача на полі (та же панель `.moment`, `MatchScreen sheet`), потом стандартная primary-кнопка «Перейти в
  роздягальню» (как «Нова кар’єра», без подписи — решение пользователя) → дошка.
  **Без единой цифры и без конкретных моментов** (решение пользователя: конкретика — дошке, макет пересекался): одно
  предложение-резюме (`summary`, ~50 вариаций по `result`/`scored`/`bad`/`tired`/`scoreless`/`strength`), обіцянка
  (`promise`: `kept`/`missed`/`untouched` — `promiseState`: вариант с `requires.flags: week_promise` зіграно + гол Реєса
  тієї ж хвилини), трибуни и затори (`crowd`: `venue`/`result`/`hype`/`weather`). Выбор — `pickFresh` с `session.flavorSeen`
  (общая память с репликами, считается до `recordResult`). У каждого вида безусловное правило, без цифр и «!» (тест
  `whistle.test.ts`); `WhistleCard` — в списке экранов теста «никаких процентов».
- **Після матчу (19.09, макет «Дошка аналітика → картка з дельтою», кадр 1Б):** итог матча — `ui/BoardScreen.tsx`
  (магнитная доска, маркер **Neucha** — `public/fonts/Neucha-*`, только для доски), потом `ui/DeltaScreen.tsx`,
  потом таблиця → стрічка → тиждень. Логика — `engine/board.ts`: `boardMoments` — три магнита
  (найкращий/найгірший из `pickMoments`, поворотний — последний гол, сменивший лидера, со сценой в ту же минуту;
  дубли не вешаются), позиция — `FAMILY_SPOT` из `Pitch.tsx`; `cardDelta` — что изменилось в картке из карьеры
  до/после `applyMatchToCareer` (голоса матча, довіра було → стало с причиной, нові сліди с минутой поступка,
  «Голос» только при смене статуса; пустые рубрики скрыты). Ничего нового не хранится. Числа аналитика —
  без процентов; полная картка со стикера открывается на месте (`stage.card`), не через `#/player` — роут
  перемонтирует Game. `ResultScreen` удалён.
- **Таблиця = сторінка ESPM** (19.09, макет «Таблиця: ESPM», `ui/SeasonScreen.tsx`, `engine/espm.ts`,
  `content/ads.json`): экран сезона — страница спортивного сайта в рамке браузера, оммаж на известное издание
  (красный прямоугольный логотип латиницей — решение пользователя). Чужая типографика нарочно: Roboto Condensed
  (`public/fonts/RobotoCondensed-*`) + системный sans, не Lora/Oswald. Заголовок тура — `roundHeadline` из данных
  (лидер, наш счёт, движение по таблице; соперник только «проти „{gen}“», род глаголов под «Вальмару»). Ирония над
  сайтом — пейвол «ESPM+» над «Аналітикою» и cookies «Як і всі», больше не добавлять. Реклама ниже сгиба —
  `ads.json`: `kind` banner/reco/classified, `when` {last, trust, position} как у титула, вес 3^ключей, память —
  `recentPosts` (общая с стрічкою), rng по сиду сезона и туру; правила контента: абсурд в форматах настоящей
  рекламы, **без реальных брендов, без букмекеров/коэффициентов, без «!»** (тест), у каждого формата ≥2
  безусловных правил. Вердикт сезона — блок под таблицей, не трогать. **Это единственный «экран в экране» —
  больше таких не делать** (решение пользователя).
- Карточка игрока (`ui/PlayerCard.tsx`) — сверху **стикер** (`ui/Sticker.tsx`, 19.09, по карточке архетипа DE):
  портрет `public/img/portrait.webp` в рамке, чёрная плашка с именем, реплика доминантного голоса, четыре
  бокса — атрибутные голоса с модификаторами (заливка у тех, что `voiceSees`), «+ ЕГО/КОМАНДА» — кого
  слушаешь, с долей словами; компактный стикер — шапка меню кар’єри (`#/play`), тап открывает картку.
  Тексты голосов — `ui/voices-text.ts`. Дальше **голоси первыми** (бачить / чутно / мовчить, Его и Команда — по тому, кого
  слушали), потом сезон, кар’єра, атрибуты по группам. Тексты голосов — в `VOICES` там же.
- **Исход называет конкретного партнёра/соперника (assist/teamGoal/concede) — обязан иметь
  `apply.scorer`**, ключ ростера, названный в тексте. Без него лента матча объявит гол случайным
  именем — тест `tests/content.test.ts` это ловит. Крит-исход (`outcomes.crit`) всегда выбирается
  через `resolve.ts:pickOutcome()`, никогда не дублировать эту проверку локально в UI.
- Реальных футболистов и тренеров по имени в контенте нет — только generic-описание техники (право
  на публичность); реальные **клубы** — только в стрічці (`posts.json`, группы `world`/`cross`), решение
  18.09. Исключение — уже устоявшиеся общие термины футбола («паненка»).
- Голос, который слушают дважды подряд (`state.voices.streak`), становится громче — не добавлять
  третий такой эффект без причины, у голосов уже есть счётчик и серия, использовать их.
- Флаги-последствия — `src/content/flags.json`; реактивные эпизоды — `rx_*` с `requires.flags`,
  в тексте допустимы `{trigger.past}` и `{trigger.minute}`; их исходы обязаны снимать свой флаг.
  Успех на риске не должен давать только минус (флаг с двумя правилами — минус и плюс).
- Условия матча (`conditions.ts`) игрок не выбирает; `neutralConditions()` — для тестов и прогона,
  нейтральные условия не добавляют ни одной строки модификатора (есть тест). Новый соперник —
  запись в `roster.json` → `opponents` с падежами и `strength`.
- **Вища ліга — окремий пул** (M17, 22.09): `requires.league: "top"` — епізод існує лише в другому сезоні (`conditions.league`,
  фільтр у `createMatch`); без поля — обидві ліги. Тема таких епізодів — ми андердоги; чек-лист ролей десятки (DESIGN M17).
  Прапори App для канви: `sub_there_again` (другий матч із клубом Ларссона), `agent_in_stands` (7-й тур S2). Міряти
  `npm run sim -- --season 20 --top` (друга половина у вищій) і `--verdicts 300 --top`.
- **Новый эпизод — сразу с 2–3 сетапами и весом:** новые оборонительные — `weight` 0.5–0.6 (в открытых
  слотах они разбавляют атаку, критерий «матчей с голом ≥ 20%» уезжает), новые атакующие — 1.2–1.4; самая
  физическая опция — 9–12 сил (критерий по стамине). После добавления — `sim 2000` и `--season 12`.
- **Установка тренера в сценах:** `when.instruction` (hold/press/free) у сетапов и `requires.instruction` у
  эпизода — «автобус», гегенпресинг, володіння читаются словами, не строкой брифинга. **Маркерные флаги**
  (`plan_bus`/`plan_press`/`plan_possession`, `viral_story`, `podcast_talk`, `flashy_week` + конкретные
  `dubai_photos`…): правило в `flags.json` с `value: 0` — только для сетапов и реактивных сцен,
  `context.ts` их пропускает. Дело недели может быть сеттером флага для `rx_` (тест это учитывает):
  побачення → `viral_story` → `rx_insta_taunt`; реактивная сцена обязана снять свой флаг. **`flashy_week` — три
  разных решения, не одна провокация** (M11, 20.09): машина → `rx_car_coach` (тренер на брівці: бігти назад / грати як
  завжди / показати швидкість), тату → `rx_tattoo_contact` (стик, який болить), афіша → `rx_poster_show` (трибуни чекають
  шоу); Дубай і бренд лишаються `rx_flashy_taunt` (сетапи на них там). Специфічні сцени знімають і свій флаг, і
  `flashy_week`, щоб за ними не йшла ще й генерична. **Сцени тижня ставлять флаги теж** (`sc_agent_call` → `scout_watching`
  з сетапами в ударних епізодах і постом; `sc_fan_confront` → `ultras_backing`, +1 спокій, сетапи, пост) — тест «правило
  ніхто не ставить» рахує `WEEK_SCENES`. **Відповіді в стрічці** — 21 пост (було 7), половина зі слідом: флаг про партнера
  (`partner_trusts`/`partner_annoyed` — впливає на `partnerBond`), `sub_threat`, `ref_annoyed`, або голос (`louder`).
- **Квота атаки выключена** (`BALANCE.match.minAttack` = 0, решение 19.09): форму матча задаёт пул под
  персонажа (АПЗ — атакующие сцены), а не планировщик. «Матчей с голом» в `sim`/`acceptance` считается по
  случайной политике — не суммарно с `always_safe`, который никогда не бьёт. Если доля голов проседает
  при расширении пула — писать атакующие сцены, не квоту.
- **Организационные эпизоды** (владение, темп, скан, хибний рух) вознаграждаются не голом: тренер +6…+8,
  трибуны ±0, `keyPass`/`duelWon`, `partner_trusts`, и `followUp` в существующий удар (`fin_shot`,
  `ep_edge_of_box`, `ep_between_lines`, `ep_progressive_pass`) с `followUpElse`. `effect: great` у них
  не ставить — момент решает цепочка.
- Свежесть реплик и ленты — двухступенчатая: `pickFresh(pool, seen, rng, seenNow)`; `seen` — сезон,
  `seenNow` — этот матч (`session.flavorSeenNow` / `feedSeenNow`). Не сводить к одному множеству: к
  12-му туру сезонная память съедает весь пул ленты, и повтор внутри матча возвращается.
- **Новая ситуация — сначала вариант сетапа, потом эпизод.** `Episode.setups[]` — `{ when, text }` с
  условием как у `flavor.json` (+ `venue`/`weather`/`strength`/`flags`), побеждает самое конкретное;
  опции и исходы не меняются. Новый эпизод — только если решение другое, а не сцена. После правки
  пула — `npm run sim -- --season 12`, цифры повторов в README обновить.
- `requires.score` — эпизод только при таком счёте; ставить `minMinute ≥ 45` (тест), иначе он зависает
  в плане. Проверяется при показе, как `notFlags`; подменить нечем — берётся свежий эпизод вне плана.
- Память между матчами — `BALANCE.match.memory`, кривая, а не список; `episodeMemory()` в истории
  отдаёт `id → возраст`. Не возвращать плоский `recentWeight`: он даёт 42% повторов к 4-му матчу.
- **Соперники — без фамилий.** `roster.json → opponents.*.players` — характеристики с падежами, только
  мужские и с `gen` = винительный («їхній ветеран», не «зірка», не «роботяга», не «фантазиста»);
  `trait` обязателен у striker/winger/mid и даёт флаг `them_<trait>` на матч с правилом в `flags.json`.
  **Черты на бросок не складываются** (`context.ts`): идёт одна, самая сильная — у «Сан-Дореа» три черты
  били по позиції в обороне на −3 (лог тестера 18.09). Остальные флаги применяются все.
  У каждой роли ≥2 `variants` (нейтральная позиция — «опорник», «хавбек», «латераль», — и образ —
  «здоровань», «лисий стоппер»); `rosterFor(key, rng)` выбирает одно имя на матч. Роль `cb` —
  защитник-антагонист для сетапов (`{them.cb}`), без характеристики. Своих — наоборот, по имени: `self`, `sub`,
  `coach`, `rb`/`lb`/`cb2`/`winger` — использовать в новом контенте, а не «дублер»/«тренер»/«захисник».
- Персонаж в ленте и протоколе — `{self}`/`apply.scorer: "self"` для своего гола; не возвращать
  безымянное «Гол!».
- Голоса имеют вес (`BALANCE.voice`, `context.ts`): голос атрибута +1, **Его +1, когда слышен** (на кураже
  или своей серии; M9, 20.09 — без этого Его был единственным голосом, которого слушать невыгодно всегда),
  серия Его/Команди −1 на «другой стороне». Команда к броску бонуса не даёт — не добавлять.
- Сезон — `engine/season.ts`: расписание, чужие результаты, таблица, вердикт; UI только читает.
  Соперник и поле матча берутся из `ourFixture()`, не из случайности `generateConditions`.
- **Цепочки.** `apply.followUp: "<id>"` — исход ведёт в следующее решение в том же слоте; рядом
  обязателен `apply.followUpElse` (что происходит, если цепочка не сработала — лимит или звено уже
  было). Звенья — `followUpOnly: true` (`fin_*`): не планируются, не всплывают, в тексте допустим
  `{trigger.past}`. Не вести цепочку из исхода, чей текст уже решил эпизод (гол) — только из «ти
  виходиш на удар / штрафний / пенальті».
- Манера удара — опция модуля `fin_shot`/`fin_penalty`, не новый эпизод. Свойство воротаря —
  `keeper.trait` в ростере + правило с `options: [...]` в `flags.json` (по id опции, не по атрибуту).
  Читаемость — через `keeper_read`; в сетапах `when.flags: ["keeper_read", "keeper_<trait>"]`.
  Условные опции — `requires.flags`; безусловных у эпизода всё равно 3–4 (тест).
- У каждого эпизода `family`; новый эпизод без семьи валит тест. Семьи давят память слабее id.
- Баланс сезона мерить не только `sim`, но и W/D/L по силе соперника (скрипт в README, раздел
  «Пересборка баланса»): цель — наугад против равных ≈ 40/20/40, против слабых ≈ 70/15/15.
  Матчей с голом игрока — не ниже 20% (критерий); свои голы ленты — не поднимать выше 0.03.
- `knock` (мікротравма) — системный флаг ленты и исходов, снимается только через `rx_knock`;
  не превращать в автоматический штраф без решения. Травм (`injured`) — не больше
  `BALANCE.injury.maxPerSeason` за сезон: дальше исход даёт `knock` (`applyChoice`, `career.injuriesSeason`).
- Доверие тренера между матчами регрессирует к старту с силой `BALANCE.coachTrustReversion` (0.25,
  было 0.4): цена «тренер −8» за дело недели должна доживать до следующего тура, а лояльность —
  дела недели с `coachTrust: +`. **Трибуни так же** (`career.fanHype`, `fanHypeReversion`, M9 20.09) —
  ресурс сезона, не эффект матча.
- **Вердикт сезона — две силы** (`seasonVerdict`, `BALANCE.season`, M9 20.09): тренер садит (низ таблицы,
  довіра < 45, оценка < 5.2), трибуны защищают (оценка ≥ 7.5 и ≥ 5 голов+асистов) или свистят (< 7.0 —
  лава раньше трансфера от таблицы); агент — за таблицу или за протокол. Текст называет причину. Мерить
  `npm run sim -- --verdicts 300`: хороший исход у каждой чистой политики в 20–80% сезонов. Не возвращать
  вердикт по одному тренеру: осторожный бот получал продовження в 99% сезонов, рисковый — лаву в 99%.
- **Лава запасних** (`career.benched`, `BALANCE.bench`): вердикт «лава» и довіра < 30 без гола/асиста
  садят; с лавы выходишь на 58-й (4 решения, `match.ts:benchWarmup`, строка `benchIn` в `feed.json`),
  возвращаешься довірою ≥ 55, голом/асистом или трибунами ≥ 8.5. Лава — петля, не приговор: пока сидишь,
  довіра регрессирует к 55. **Сцена «розминайся»** (21.09, решение пользователя: новичок не понимал, почему лента идёт
  без него): на `bench.callMinute` (52′) слот вне планировщика с `bench.callEpisode` (`ep_bench_call`, `requires.flags:
  on_bench` — системный флаг движка, стоит до выхода) — тренер зовёт разминаться, три варианта задают, каким выйдешь
  (кураж / Спокій и довіра / `seen_from_bench` +1 бачення·позиція на матч; катастрофа ривків — `knock`). Лента первого
  тайма теперь прокручивается до перерыва, дальше обычный `advanceTo`; пока `session.onBench` — силы не тратятся;
  `benchEnter` на 58-й: свежие ноги, флаг снят, «виходиш». В первом матче это нулевой прожектор «Ти на лаві» (5 подсказок).
  **Вихід — подія** (21.09, `engine/entry.ts`, `content/entry.json`, `ui/EntryCard.tsx`): на хвилині виходу лента
  зупиняється (`App.proceed`: `advanceTo` до `entryMinute`, pending `entry`), поверх — лист без кубика: сетап
  (табло, четвертий суддя, тренер; варіанти за рахунком, станом арки, `agent_left/stayed`) і голоси колонкою, кнопка
  «Вийти на поле» (той самий стиль, що «Перейти в роздягальню»). Дебют — усі голоси, крім Спокою (його ще не чутно —
  тиша на його місці навмисна), Тіло — про коліно (рішення користувача: конкретніше за «ногу»); звичайний вихід —
  до трьох голосів, спершу ті, що про цей матч (`when` як у сетапів: рахунок, `tired`, `knock`, `arcMin`, кураж).
- **Партнер з пам’яттю** (M11, 20.09): `career.partnerBond` = сума приводів за кар’єру (`MatchState.people.partner`:
  +1 на `partner_trusts`, −1 на `partner_annoyed` з ісходів; флаги тижня рахуються при вході в матч, принесені з
  минулого матчу — ні). Пороги `BALANCE.people` (3 / −3) дають системні флаги `partner_bonded` / `partner_cold`
  (`career.ts:peopleFlags`) на матч (через `consumeStartPenalty`), тиждень і стрічку: правила в `flags.json` (±1 пас/бачення
  в атаці), сетапи в 6 епізодах з партнером, репліки КОМАНДИ на clean/fail, акаунт `@moraes_ten` і пости в стрічці.
  У тесті «правило ніхто не ставить» ці два — системні. Наступні люди (дублер, тренер) — тим самим шляхом: лічильник у
  `people`, поріг у `BALANCE.people`, флаг у `peopleFlags`.
- Флаги про людей (`CARRIED_FLAGS` в career.ts) переживают свисток; в текстах реактивных эпизодов —
  `{trigger.when}` / `{trigger.When}`, а не «на {trigger.minute}-й»: для перенесённого флага это
  «ще минулого матчу». Каждый флаг из контента — либо правило в `flags.json`, либо системный
  (`booked`/`injured`/`sent_off`/`tired`); правило без эпизода, который его ставит, валит тест.

## Команды

```
npm run dev      # localhost:5179 (см. .claude/launch.json)
npm test         # 298 тестів: дві ліги, відпустка, фінал, вихід із лави, арка, пролог, перший матч, агент, фінальний свисток, партнер з пам’яттю, движок, контент, условия, M3, career, сезон, лига, цепочки, реплики, лента, голос бачить, тиждень, тиждень v3, стрічка, слоты карьеры, титул, розв’язка на поле, програмка, дошка/дельта, ESPM, критерии приёмки
npm run sim      # балансный прогон ботами; --random-conditions — со случайными условиями матча
npm run sim -- --season 12   # повторы эпизодов и текстов сетапа на дистанции сезона
npm run sim -- --weeks 150   # политики недели між матчами на дистанции сезона
npm run sim -- --verdicts 300 --top   # те саме у вищій лізі (другий сезон)
npx tsx tools/scene-rate.ts 300   # частота и повторы сцен-продолжений недели
npm run qa -- 300            # QA-прогон карьер: инварианты (флаги, атрибуты, неделя, уровень), исключения
npm run audit                # аудит опций: доминируемые варианты и эпизоды с «правильным ответом»; --all, --bonus=3
npm run build    # dist/ — деплоится на GitHub Pages workflow'ом
```
